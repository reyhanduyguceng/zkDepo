def main(private field orderId, field quantity, field totalCost, field productPrice) -> field{
    field expectedTotalCost = quantity * productPrice ;
    assert(totalCost == expectedTotalCost);
    return orderId;
}